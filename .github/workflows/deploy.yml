name: Deploy Website

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

  jobs:
   deploy:
     runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # REPLACE YOUR OLD "DEPLOY TO EC2" BLOCK WITH THIS:
      - name: Debug Connection
        run: |
          echo "${{ secrets.EC2_SSH_PRIVATE_KEY }}" > private_key.pem
          chmod 600 private_key.pem
          ssh -o StrictHostKeyChecking=no -i private_key.pem -v ubuntu@${{ secrets.EC2_HOST }} "echo 'Connection successful!'"
